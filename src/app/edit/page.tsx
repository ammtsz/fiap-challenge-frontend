import { Divider, PageTitle } from "@/components";

const Edit = () => {
  return (
    <main className="page">
      <PageTitle title="Editar Postagem"/>
      <Divider />
    </main>
  )
};

export default Edit;